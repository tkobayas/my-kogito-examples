package org.drools.example;

import org.drools.example.Person;
import org.drools.example.Order;
import org.drools.example.Result;

rule "Hello"
  when
    Person()
    Order()
    $r : Result()
  then
    $r.addValue("Hello");
end
